import * as React from "react";
import { Typography } from "@material-ui/core";

const OrderingDisabled: React.SFC = () => (
  <Typography variant="h4">Ordering Not Available</Typography>
);
export default OrderingDisabled;
