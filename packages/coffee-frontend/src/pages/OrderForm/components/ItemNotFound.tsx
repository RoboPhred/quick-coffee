import * as React from "react";
import { Typography } from "@material-ui/core";

const ItemNotFound: React.SFC = () => (
  <Typography variant="h6">Item Not Found</Typography>
);
export default ItemNotFound;
